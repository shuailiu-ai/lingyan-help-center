# Public base URL used for canonical links, Open Graph metadata, robots.txt and sitemap.xml.
# Keep the optional path prefix when the site is eventually hosted below /help.
NEXT_PUBLIC_SITE_URL=https://lingyansci.com/help
